"use strict";
const s = 'hii I am string';
const num = 40;
const bool = false;
const student = {
    name: "raj",
    age: 22,
    isStudent: true,
};
console.log(`${typeof (s)}  ${s}`);
console.log(`${num}:${typeof (num)} `);
console.log(`${bool}:${typeof (bool)} `);
console.log(`${student.name}:${typeof (student)} `);
console.log(student);
